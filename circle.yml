machine:

  timezone:
    America/Los_Angeles

  java:
    version:
      oraclejdk8

general:
  branches:
    only:
      - master # only build/deploy from master

test:
  # Call './gradlew clean build pTML' instead of the default 'mvn integration-test'
  override:
    - ./gradlew clean build pTML
